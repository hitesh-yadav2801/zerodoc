abstract final class AppDurations {
  static const screenTransition = Duration(milliseconds: 280);
  static const bottomSheet = Duration(milliseconds: 320);
  static const cardPress = Duration(milliseconds: 120);
  static const successDraw = Duration(milliseconds: 400);
  static const snackbar = Duration(seconds: 3);
  static const quickFade = Duration(milliseconds: 150);
  static const springBack = Duration(milliseconds: 200);
}
